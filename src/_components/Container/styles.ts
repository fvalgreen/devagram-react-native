import {StyleSheet} from "react-native";


const {colors} = require("../../../app.json");

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: colors.corCinza5,    
  },
  content: {
    flex: 1,
    backgroundColor: colors.corCinza5, 
  }  
});

export default styles;
